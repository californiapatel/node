<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo List</title>
</head>
<body style="background-color: #FAF7F0; height: 100vh; display: flex; justify-content: center; align-items: center; flex-direction: column; margin: 0; font-family: Arial, Helvetica, sans-serif;">
    <h1 style="color: #705C53; margin-bottom: 20px; font-family: 'Times New Roman', Times, serif; font-size: 50px;">Todo List</h1>
    <ul style="list-style-type: none; padding: 0; width: 100%; max-width: 600px;">
        <% todo.forEach(el => { %>
            <li style="display: flex; justify-content: space-between; align-items: center; color: #FAF7F0; background-color: #4A4947; padding: 15px; margin-bottom: 10px; border-radius: 8px; box-shadow: 0 4px 8px rgba(0,0,0,0.1);">
                <%= el.task %>
                <a href="/edit/<%= el.id %>" style="color: #D8D2C2; text-decoration: none;" >Edit</a>
                <form action="/delete/<%= el.id %>" method="POST">
                    <button type="submit" style="height: 50px; width: 120px; border-radius: 25px; border: none; background-color: #D8D2C2; color: #333; font-weight: 500;">Delete</button>
                </form>
            </li>
        <% }) %>
    </ul>
    <a href="/add" style="color: #D8D2C2; text-decoration: none; height: 50px; width: 150px; border-radius: 30px; border: none; background-color: #4A4947; text-align: center; line-height: 50px;" >Add Task</a>
</body>
</html>
 